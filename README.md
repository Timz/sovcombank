# sovcombank

## Project setup
```
yarn install
```

### Compiles and hot-reloads for development
```
yarn serve
```

### Compiles and minifies for production
```
yarn build
```

### Lints and fixes files
```
yarn lint
```

### TODO
Использовался данный запрос:
https://developer.oxforddictionaries.com/documentation#!/Search/get_search_thesaurus_source_lang
он возвращает не все необходимые данные (нет фильтра части речи, обозначения и транскрипции)
а запрос /api/v2/entries/{source_lang}/{word_id} с нужными данными не ищет слова по маске

+Поиск по первым и выдача первых 10 слов списком
+Сортировку списка осуществить в алфавитном порядке
+Если обозначение не влезает, обрезать его
+При клике на слово можно открыть и увидеть больше обозначений и транскрипцию (данные в апи не получил поэтому
подставил заглушку для демонстрации)
+Слова можно сохранять в избранное
+Сессия должна сохраняться и не сбрасываться при обновлении страницы и перезагрузке браузера
+На странице избранных слов можно поменять порядок слов перетаскиванием и удалить слово при клике на
звездочку
+Поиск должен работать и в странице избранных слов
-Должен работать фильтр части речи (в Api не нашел данную опцию )
